apiVersion: v1
kind: Secret
metadata:
  name: tailscale-auth
  namespace: llm-proxy
type: Opaque
stringData:
  # PLACEHOLDER: Replace with a real pre-auth key from Headscale.
  # kubectl -n llm-proxy delete secret tailscale-auth --ignore-not-found
  # kubectl -n llm-proxy create secret generic tailscale-auth --from-literal=TS_AUTHKEY="<real-key>"
  TS_AUTHKEY: "REPLACE_WITH_HEADSCALE_PREAUTH_KEY"
